pipeline {
    agent { node { label 'maven' } }
    environment { APP_NAMESPACE = 'yrzkko-recover' }
    stages {
        stage('Test') {
            options { timeout(time: 60, unit: 'SECONDS') }
            steps {
                dir('shopping-cart-v2') {
                    sh './mvnw clean test'
                }
            }
        }
    }
}